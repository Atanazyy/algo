#include <bits/stdc++.h>

using namespace std;

const int r = 1e5 + 9;
int tab[r];
int sto[r];

int main()
{
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++)
    {
        cin >> tab[i];
        sto[tab[i]]++;
    }
    for(int i = 1; i <= n; i++)
    {
        if(tab[i] != tab[])
    }
}