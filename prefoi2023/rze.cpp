#include <bits/stdc++.h>

using namespace std;

long long tab[2009];
bool dp[2009][2009];

int main()
{
    int n, a, b;
    cin >> n >> a >> b;
    for(int i = 1; i <= n; i++)
        cin >> tab[i];
    int (bit = 50; bit >= 0; bit--)
    {

    }
}
