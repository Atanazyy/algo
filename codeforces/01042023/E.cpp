#include <bits/stdc++.h>

using namespace std;

int main()
{
    long double p, q, r;
    cin >> p >> q >> r;
    if(p == 0 && q == 0 && r == 0)
        cout << "1792283455048516";
    else
        cout << "0";
}